<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aorora">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MindfullnessBreathingGame"></activity>
        <activity android:name=".mindfullnessBreathingGame" />
        <activity android:name=".MindfullnessBreathing" />
        <activity android:name=".ButterflyDetailsPage" />
        <activity android:name=".ButterflyCollectionPage" />
        <activity android:name=".ProfilePage" />
        <activity android:name=".CommunityPage" />
        <activity android:name=".HomeScreen" />
        <activity android:name=".MindfullnessSelection" />
        <activity android:name=".SurveyPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>